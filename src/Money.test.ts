import { Money } from './Money';

describe('Money Object', () => {

    describe('allocateMoneyFor', () => {

        beforeAll() {
            
        }

        it('should correctly allocate coins for sufficient amout', () => {

        }); 
    });

})